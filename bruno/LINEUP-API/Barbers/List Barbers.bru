meta {
  name: List Barbers
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/barbers?unitId={{unitId}}
  body: none
  auth: none
}

params:query {
  unitId: {{unitId}}
}

headers {
  Authorization: Bearer {{accessToken}}
}

vars:pre-request {
  baseUrl: http://localhost:3000
}
